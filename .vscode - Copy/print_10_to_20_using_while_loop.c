#include<stdio.h>
int main()
{
    int a= 0;
    //here we need to print values from 10 to 20
    //therfor we give the if condition in while loop
    while(a<=20){
        //these will return the values from 0 to 20 but
        // we need 10 to 20 therefor we gives if condition.
      //  if(a>=10)
      //  {
        printf("%d\n",a);
       // }
        a++;
    }
    return 0;
    
}