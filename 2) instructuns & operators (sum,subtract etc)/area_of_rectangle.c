#include<stdio.h>
int main()
{
    float l,b;
    printf("the length of rectangle :");
    scanf("%f",&l);

    printf("the breadth of rectangle :");
    scanf("%f",&b);
     
    printf("the area of rectangle %f",l*b);
    return 0;

}